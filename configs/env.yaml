# Environment + constraint settings (mirror of experiment.yaml sections).
# Scripts read configs/experiment.yaml; this file documents the env block.
environment:
  transaction_cost_bps: 2.0
  lookback: 26
constraints:
  long_only: true
  max_weight: 0.60
  turnover_cap: 0.50
  gross_cap: 1.0
  cash_index: null
  cash_floor: 0.0
risk:
  cvar_alpha: 0.95
  cvar_limit: 0.03
  cvar_window: 52
